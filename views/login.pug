doctype html
html
    head
        title Dang Nhap
        link(rel="stylesheet" href="/css/styles.css")
    body
        .login-container
            h1 Dang Nhap
            form(action="/login" method="POST")
            .form-group
            label(for="username") Ten tai khoan
            input#username(type="text" name="username" placeholder="Nhập tên tài khoản"
            required)
            .form-group
            label(for="password") Mat khau
            input#password(type="password" name="password" placeholder="Nhập mật khẩu"
            required)
            .form-group
            button(type="submit") Dang Nhap
            .form-group
            a(href="/register") Dang ky tai khoan moi